export const prasCommands = () => {
    const data = [
        {
            name: 'django',
            description: 'Set up a new Django project with all configurations in a single command'
        },
        {
            name: 'netfetch',
            description: 'Scan the network to retrieve information on connected devices or locate a specific device'
        }
    ];

    return data;
}